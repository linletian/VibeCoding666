# VibeCoding666 架构复盘（2026-02-13）

## 目标与结论

目标：评估一个可兼容 macOS / Windows / Linux 的轻量化软键盘工具，当前架构是否合理，是否存在“依赖臃肿”问题。  
结论：当前架构对产品目标是合理的；问题核心不在依赖数量，而在 Electron 体积天然较大、主进程职责集中、以及原生输入模块维护风险。

## 当前事实（基于代码库）

- 顶层依赖仅 3 个：`electron`、`electron-builder`、`robotjs`
- 代码体量较小：
  - `src/main.js`：457 行
  - `src/renderer/keyboard.js`：194 行
  - `src/renderer/config.js`：370 行
  - `src` 目录约 68KB
- 开发态 `node_modules` 体积约 414MB（主要来自 Electron 生态，而非业务代码）

## 架构合理性判断

### 合理的部分

- Electron 作为跨平台桌面壳，能够统一三平台窗口行为和分发流程
- 主/渲染进程分离并通过 IPC 通信，基本模式正确
- 配置读写、导入导出、全局快捷键等核心能力已闭环
- 对 `robotjs` 缺失有降级处理（不会直接崩溃）

### 需要关注的部分

1. 主进程职责偏重  
`src/main.js` 同时处理窗口管理、配置管理、输入模拟、IPC 路由和文件导入导出，后续维护成本会升高。

2. 安全与升级负担  
主窗口启用了：
- `nodeIntegration: true`
- `contextIsolation: false`
- `enableRemoteModule: true`  
这会增加安全风险并提高未来升级成本。

3. 原生模块单点风险  
`robotjs` 是核心能力来源，同时也是跨平台编译和兼容问题的主要来源。

4. 配置默认值来源分散  
默认按键在主进程和配置页存在两套定义，未来容易发生配置漂移。

5. 遗留接口可能存在  
主进程监听了 `move-window` 与 `snap-to-edge`，渲染侧调用不明显，建议后续核查和清理。

## “臃肿感”的来源解释

- 并非依赖数量多，而是 Electron 的基础体积大
- 打包配置显式包含 `node_modules/**/*`，视觉上会产生“全量打包”的臃肿印象
- `simple` 方案中仍出现 Electron 相关依赖痕迹，强化了“组件很多”的感受

## 暂不执行、但建议保留的未来优化路线

### P0（低风险高收益）

1. 拆分主进程模块  
建议拆为：
- `config-store`
- `window-manager`
- `input-service`
- `ipc-router`

2. 收敛默认配置源  
将默认键位和基础设置抽到共享模块，主/渲染共同引用。

3. 清理无效 IPC 与死代码  
核查并删除未使用事件和冗余逻辑。

### P1（中期稳定性）

1. 引入 `preload` 桥接  
逐步迁移到 `contextIsolation: true`，只暴露白名单 API。

2. 评估 `robotjs` 替代或双实现策略  
为高风险平台提供兼容兜底实现，降低单点故障概率。

3. 精简 simple 子项目依赖与说明  
避免“轻量方案仍依赖 Electron”的认知冲突。

### P2（发布体验）

1. 优化打包 `files` 规则和产物审计
2. 为三平台建立最小回归清单（手工或脚本化）
3. 补充版本升级兼容记录（Electron 与原生模块）

## 最终判断

- 若目标是“尽快稳定交付三平台桌面工具”，当前架构可继续演进，不需要推倒重来。
- 若目标是“极致轻量 + 极低维护成本”，应优先做主进程拆分与安全模型升级，再评估输入层替代方案。

